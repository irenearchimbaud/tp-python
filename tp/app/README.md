# Library API

API REST de gestion de bibliothèque développée avec FastAPI.

## Installation
Se mettre a la racine du projet
```bash
python -m pip install -r tp/app/requirements.txt
uvicorn app.main:app --reload